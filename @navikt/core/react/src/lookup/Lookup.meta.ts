import type { ComponentMetadata } from "../utils/types/metadata";
import { Lookup } from "./index";

const metadata: ComponentMetadata = {
  name: "Lookup",
  components: {
    Lookup,
  },
  keywords: ["lookup", "search", "søk", "autocomplete", "typeahead"],
};

export { metadata };

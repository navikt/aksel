import * as React from 'react';
import AlertStripe, { AlertStripeProps } from './alertstripe';

class AlertStripeFeilInline extends React.Component<AlertStripeProps> {
    render() {
        return <AlertStripe type="feil" inline={true} {...this.props} />;
    }
}

export default AlertStripeFeilInline;

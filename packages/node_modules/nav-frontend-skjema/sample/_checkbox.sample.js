import { Checkbox } from './../';
import generateSample from '../../../../guideline-app/app/utils/sampling/sampleDataGenerator';

export default generateSample(Checkbox, ['disabled', 'feil'], { label: 'Checkboks', id: 'detIDenskalvaere' });

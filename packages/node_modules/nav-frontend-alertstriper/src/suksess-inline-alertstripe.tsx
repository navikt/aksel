import * as React from 'react';
import AlertStripe, { AlertStripeProps } from './alertstripe';

class AlertStripeSuksessInline extends React.Component<AlertStripeProps> {
    render() {
        return <AlertStripe type="suksess" inline={true} {...this.props} />;
    }
}

export default AlertStripeSuksessInline;

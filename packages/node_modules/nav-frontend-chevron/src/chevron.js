import React, { Component, PropTypes as PT } from 'react';
import classNames from 'classnames';
import 'nav-frontend-chevron-style'; // eslint-disable-line import/extensions

const cls = (orientasjon, stor, className) => classNames('nav-frontend-chevron chevronboks', className,
    {
        'chevron--stor': stor
    },
    {
        'chevron--hoyre': orientasjon === 'høyre',
        'chevron--venstre': orientasjon === 'venstre',
        'chevron--ned': orientasjon === 'ned',
        'chevron--opp': orientasjon === 'opp'
    });

/**
 * Basiskomponent for Chevron
 * For å få chevron i en spesifikk rettning bruk de rettningsspesifikke komponentente:
 * HoyreChevron, VenstreChevron, OppChevron, NedChevron.
 */
class NavFrontendChevron extends Component { // eslint-disable-line react/prefer-stateless-function
    render() {
        const { orientasjon, stor, className, ...props } = this.props;
        return (
            <i className={cls(orientasjon, stor, className)} {...props} />
        );
    }
}

NavFrontendChevron.defaultProps = {
    orientasjon: 'høyre',
    stor: false,
    className: ''
};

NavFrontendChevron.propTypes = {
    /**
     * Retting chevron skal peke
     */
    orientasjon: PT.oneOf(['høyre', 'venstre', 'opp', 'ned']),
    /**
     * Sett denne proppen hvis chevron skal være stor
     */
    stor: PT.bool,
    /**
     * Klassenavn for chevron
     */
    className: PT.string
};

export default NavFrontendChevron;

export { default as HoyreChevron } from './hoyre-chevron';
export { default as VenstreChevron } from './venstre-chevron';
export { default as OppChevron } from './opp-chevron';
export { default as NedChevron } from './ned-chevron';

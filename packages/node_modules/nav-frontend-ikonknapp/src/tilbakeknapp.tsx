import * as React from 'react';
import Ikon from 'nav-frontend-ikoner-assets';

import Ikonknapp from './ikonknapp';
import { IkonknappProps } from './ikonknapp';

class Tilbakeknapp extends React.Component<IkonknappProps> {
    render() {
        return (
        	<Ikonknapp
        		ikon={<Ikon kind="chevron-venstre" />}
        		type="flat"
                kompakt
        		{...this.props}
        	>
                {this.props.children || 'Tilbake'}
            </Ikonknapp>
        );
    }
}

export default Tilbakeknapp;

import * as React from 'react';
import AlertStripe, {AlertStripeProps} from './alertstripe';

function AlertStripeSuksessSolid(props: AlertStripeProps) {
    return <AlertStripe type="suksess" solid {...props} />;
}

(AlertStripeSuksessSolid as React.StatelessComponent).propTypes = {};

export default AlertStripeSuksessSolid;

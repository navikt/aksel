import * as React from 'react';
import AlertStripe, { AlertStripeProps, AlertStripeType } from './alertstripe';

export interface AlertStripeAdvarselProps extends AlertStripeProps {
    role?: string;
}

class AlertStripeAdvarsel extends React.Component<AlertStripeAdvarselProps> {
    public static defaultProps: Partial<AlertStripeAdvarselProps> = {
        role: undefined,
    };
    render() {
        return <AlertStripe type={AlertStripeType.advarsel} {...this.props} />;
    }
}

export default AlertStripeAdvarsel;

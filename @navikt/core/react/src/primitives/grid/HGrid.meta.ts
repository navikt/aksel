import type { ComponentMetadata } from "../../utils/types/metadata";
import { HGrid } from "./index";

const metadata: ComponentMetadata = {
  name: "HGrid",
  components: {
    HGrid,
  },
  keywords: ["hgrid", "grid", "layout", "primitive", "columns"],
  related: ["HStack", "VStack", "Box"],
};

export { metadata };

import * as React from 'react';
import { StatelessComponent } from 'react';
import AlertStripe, {AlertStripeProps} from './alertstripe';

const AlertStripeNavAnsatt: StatelessComponent<AlertStripeProps> = props => (
    <AlertStripe type="nav-ansatt" size="5em" {...props} />
);

AlertStripeNavAnsatt.propTypes = {};

export default AlertStripeNavAnsatt;

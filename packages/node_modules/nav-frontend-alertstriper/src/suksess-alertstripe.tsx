import * as React from 'react';
import AlertStripe, { AlertStripeProps, AlertStripeType } from './alertstripe';

class AlertStripeSuksess extends React.Component<AlertStripeProps> {
    render() {
        return <AlertStripe type={AlertStripeType.suksess} {...this.props} />;
    }
}

export default AlertStripeSuksess;

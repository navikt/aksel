// eslint-disable-next-line
import exampleCode from 'raw-loader!./_modal.no-transpilation';
// eslint-disable-next-line
import ModalEksempel from 'babel-loader!./_modal.no-transpilation';
import generateSample from '../../../guideline-app/app/utils/sampling/sampleDataGenerator';

export default generateSample({
    baseType: ModalEksempel,
    tabOptions: {
        react: { show: false },
        html: { show: false },
        css: { show: false },
        js: { show: true, code: exampleCode }
    }
});

import React from 'react';
import { storiesOf } from '@kadira/storybook';
import { JSDokumentasjon } from './../../dokumentasjon';
import readme from './README.md';
import pkg from './package.json';
import NavFrontendSpinner from './src/spinner';

storiesOf('Nav-frontend-spinner', module)
    .addWithSections('Default', () => (
        <div>
            <NavFrontendSpinner storrelse="xxs" />
            <NavFrontendSpinner storrelse="xs" />
            <NavFrontendSpinner storrelse="s" />
            <NavFrontendSpinner storrelse="m" />
            <NavFrontendSpinner storrelse="l" />
            <NavFrontendSpinner storrelse="xl" />
            <NavFrontendSpinner storrelse="xxl" />
            <NavFrontendSpinner storrelse="xxxl" />
        </div>
    ), JSDokumentasjon(pkg, readme, NavFrontendSpinner));

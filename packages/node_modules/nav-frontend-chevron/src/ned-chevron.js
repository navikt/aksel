import React from 'react';
import Chevron from './chevron';

function NedChevron(props) {
    return (
        <Chevron type="ned" {...props} />
    );
}

NedChevron.propTypes = {};

export default NedChevron;

import * as React from 'react';
import AlertStripe, { AlertStripeProps } from './alertstripe';

class AlertStripeInfoInline extends React.Component<AlertStripeProps> {
    render() {
        return <AlertStripe type="info" inline={true} {...this.props} />;
    }
}

export default AlertStripeInfoInline;

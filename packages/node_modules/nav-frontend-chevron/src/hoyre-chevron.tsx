import * as React from 'react';
import Chevron, { NavFrontendChevronProps } from './chevron';

function HoyreChevron(props: NavFrontendChevronProps) {
    return <Chevron {...props} type="høyre" />;
}

(HoyreChevron as any).propTypes = {};

export default HoyreChevron;

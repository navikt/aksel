import { ToggleKnapp } from './../';
// eslint-disable-next-line
// import exampleCode from 'raw-loader!./_tabs.no-transpilation';
import generateSample from '../../../../guideline-app/app/utils/sampling/sampleDataGenerator';

export default generateSample({
    baseType: ToggleKnapp,
    attrs: {
        children: 'Toggle'
    },
    modifierNames: ['kompakt'],
    tabOptions: {
        react: { show: true },
        html: { show: true },
        css: { show: true }
    }
});

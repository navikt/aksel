// eslint-disable-next-line
import exampleCode from 'raw-loader!./_radiopanelgruppe.sample.no-transpilation';
// eslint-disable-next-line
import RadioGruppePanelExample from 'babel-loader!./_radiopanelgruppe.sample.no-transpilation';

import generateSample from '../../../../guideline-app/app/utils/sampling/sampleDataGenerator';

export default generateSample({
    baseType: RadioGruppePanelExample,
    tabOptions: {
        react: { show: false },
        js: { show: true, code: exampleCode }
    }
});

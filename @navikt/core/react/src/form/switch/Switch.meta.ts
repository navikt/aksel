import type { ComponentMetadata } from "../../utils/types/metadata";
import { Switch } from "./index";

const metadata: ComponentMetadata = {
  name: "Switch",
  components: {
    Switch,
  },
  keywords: ["switch", "bryter", "toggle", "form", "input"],
  related: ["Checkbox"],
};

export { metadata };

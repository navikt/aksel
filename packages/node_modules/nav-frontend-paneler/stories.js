import React from 'react';
import { storiesOf, action } from '@kadira/storybook';
import { Panel, Ekspanderbartpanel } from './src';

storiesOf('Nav-frontend-paneler', module)
    .add('Default', () => (
        <div style={{ padding: '1rem', backgroundColor: '#efefef' }}>
            <Panel>Dette er ett panel</Panel>
            <Ekspanderbartpanel tittel="Test" onClick={action('ekspander')}>
                Dette er ett expandpanel
            </Ekspanderbartpanel>
        </div>
    ));

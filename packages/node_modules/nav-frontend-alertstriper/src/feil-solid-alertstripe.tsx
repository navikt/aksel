import * as React from 'react';
import AlertStripe, { AlertStripeProps, AlertStripeType } from './alertstripe';

class AlertStripeFeilSolid extends React.Component<AlertStripeProps> {
    render() {
        return <AlertStripe type={AlertStripeType.feil} solid={true}  {...this.props } />;
    }
}

export default AlertStripeFeilSolid;

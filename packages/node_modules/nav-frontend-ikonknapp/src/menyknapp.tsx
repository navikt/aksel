import * as React from 'react';
import Ikon from 'nav-frontend-ikoner-assets';

import Ikonknapp from './ikonknapp';
import { IkonknappProps } from './ikonknapp';

class Menyknapp extends React.Component<IkonknappProps> {
    render() {
        return (
        	<Ikonknapp
        		ikon={<Ikon kind="chevron-ned" />}
                kompakt
                ikonHøyre
        		{...this.props}
        	>
                {this.props.children || 'Meny'}
            </Ikonknapp>
        );
    }
}

export default Menyknapp;

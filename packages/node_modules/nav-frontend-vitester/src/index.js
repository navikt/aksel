import React, { PropTypes as PT } from 'react';

function NavFrontendVitester({ children }) {
    return (
        <div className="nav-frontend-vitester">
            <h2>Hello from nav-frontend-vitester</h2>
            <h4>Skal publisere nå</h4>
            <h4>Skal publisere nå</h4>
            {children}
        </div>
    );
}

NavFrontendVitester.propTypes = {
    children: PT.node.isRequired
};

export default NavFrontendVitester;

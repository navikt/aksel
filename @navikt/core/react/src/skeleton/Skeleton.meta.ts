import type { ComponentMetadata } from "../utils/types/metadata";
import { Skeleton } from "./index";

const metadata: ComponentMetadata = {
  name: "Skeleton",
  components: {
    Skeleton,
  },
  keywords: ["skeleton", "placeholder", "loading", "laster", "shimmer"],
  related: ["Loader", "ProgressBar"],
};

export { metadata };

import * as React from 'react';
import { StatelessComponent } from 'react';
import EtikettBase, { EtikettBaseProps } from './index';

const EtikettInfo: StatelessComponent<EtikettBaseProps> = (props) => (
    <EtikettBase type="info" {...props} />
);

EtikettInfo.propTypes = {};

export default EtikettInfo;

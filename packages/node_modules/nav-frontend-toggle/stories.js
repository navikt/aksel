import React from 'react';
import { storiesOf } from '@kadira/storybook';
import { JSDokumentasjon } from './../../dokumentasjon';
import readme from './README.md';
import pkg from './package.json';
import Toggle from './src/toggle';

const handleOnChange = () => {
};

storiesOf('Toggle', module)
    .addWithSections('Default', () => (
        <div>
            <Toggle
                onChange={handleOnChange}
                name="toggleTestGruppe1"
                buttons={
                [
                        { label: 'Knapp en', id: 'knappEnEn' },
                        { label: 'Knapp to', id: 'knappEnTo' }
                ]
                }
            />
            <Toggle
                onChange={handleOnChange}
                name="toggleTestGruppe2"
                defaultValgt="knappToTo"
                buttons={
                [
                        { label: 'Knapp en', id: 'knappToEn' },
                        { label: 'Knapp to', id: 'knappToTo' },
                        { label: 'Knapp tre', id: 'knappToTre' },
                        { label: 'Knapp fire', id: 'knappToFire' }
                ]
                }
            />
            <Toggle
                onChange={handleOnChange}
                name="toggleTestGruppe3"
                defaultValgt="knappTreTre"
                buttons={
                [
                        { label: 'Knapp en', id: 'knappTreEn' },
                        { label: 'Knapp to', id: 'knappTreTo' },
                        { label: 'Knapp tre', id: 'knappTreTre' },
                        { label: 'Knapp fire', id: 'knappTreFire' }
                ]
                }
            />
        </div>
    ), JSDokumentasjon(pkg, readme, Toggle));

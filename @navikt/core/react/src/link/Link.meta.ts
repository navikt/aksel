import type { ComponentMetadata } from "../utils/types/metadata";
import { Link } from "./index";

const metadata: ComponentMetadata = {
  name: "Link",
  components: {
    Link,
  },
  keywords: ["link", "lenke", "anchor", "navigation", "href"],
  related: ["LinkPanel", "LinkCard"],
};

export { metadata };

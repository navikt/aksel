import type { ComponentMetadata } from "../../utils/types/metadata";
import { Bleed } from "./index";

const metadata: ComponentMetadata = {
  name: "Bleed",
  components: {
    Bleed,
  },
  keywords: ["bleed", "layout", "negative margin", "primitive", "spacing"],
  related: ["Box"],
};

export { metadata };

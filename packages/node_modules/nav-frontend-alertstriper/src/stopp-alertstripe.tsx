import * as React from 'react';
import AlertStripe, {AlertStripeProps} from './alertstripe';

class AlertStripeStopp extends React.Component<AlertStripeProps> {
    render() {
        return <AlertStripe type="stopp" {...this.props} />;
    }
}

export default AlertStripeStopp;

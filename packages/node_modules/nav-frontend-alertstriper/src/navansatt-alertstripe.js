import React from 'react';
import AlertStripe from './alertstripe';

function AlertStripeNavAnsatt(props) {
    return <AlertStripe type="nav-ansatt" size="5em" {...props} />;
}

AlertStripeNavAnsatt.propTypes = {};

export default AlertStripeNavAnsatt;

import * as React from 'react';
import Chevron, { NavFrontendChevronProps } from './chevron';

function VenstreChevron(props: NavFrontendChevronProps) {
    return <Chevron {...props} type="venstre" />;
}

(VenstreChevron as any).propTypes = {};

export default VenstreChevron;

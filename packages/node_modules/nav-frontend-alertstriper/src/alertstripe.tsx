import * as PT from 'prop-types';
import * as React from 'react';
import * as classNames from 'classnames';
import { Normaltekst } from 'nav-frontend-typografi';
import Ikon, { icons } from 'nav-frontend-ikoner-assets';

import 'nav-frontend-alertstriper-style';

export type AlertStripeType = 'info' | 'suksess' | 'advarsel' | 'feil';

const cls = (type, inline, className) =>
    classNames('alertstripe', className, {
        'alertstripe--inline': inline,
        'alertstripe--info': type === 'info',
        'alertstripe--suksess': type === 'suksess',
        'alertstripe--advarsel': type === 'advarsel',
        'alertstripe--feil': type === 'feil'
    });

const ikonKind = (type: AlertStripeType): icons => {
    const kindMap = {
        'info-sirkel-fyll': type === 'info',
        'ok-sirkel-fyll': type === 'suksess',
        'advarsel-sirkel-fyll': type === 'advarsel',
        'feil-sirkel-fyll': type === 'feil'
    };

    return Object.keys(kindMap)
        .map((key) => ({ key, predicate: kindMap[key] }))
        .find(({ predicate }) => predicate)!.key as icons;
};

export interface AlertStripeProps {
    /**
     * -
     */
    children: React.ReactNode | React.ReactChild | React.ReactChildren;
    /**
     * Bestemmer om Alertstripen skal rendres inline, uten bakgrunnsfarge
     */
    inline?: boolean;
    /**
     * -
     */
    size?: string | number;
    /**
     * -
     */
    className?: string;
}

export interface AlertStripeBaseProps extends AlertStripeProps {
    /**
     * Varianter å velge mellom
     */
    type: AlertStripeType;
}

type Props = AlertStripeBaseProps & React.HTMLAttributes<HTMLDivElement>;

class AlertStripe extends React.Component<Props> {
    render() {
        const { type, inline, size, className, children, ...props } = this.props;

        return (
            <div className={cls(type, inline, className)} {...props} >
                <span className="alertstripe__ikon" aria-label={type}>
                    <Ikon kind={ikonKind(type)} size={size} />
                </span>
                <Normaltekst className="alertstripe__tekst" tag="div">
                    {children}
                </Normaltekst>
            </div>
        );
    }
}

(AlertStripe as React.ComponentClass).propTypes = {
    children: PT.node.isRequired,
    type: PT.oneOf(['info', 'suksess', 'advarsel', 'feil']).isRequired,
    inline: PT.bool,
    size: PT.oneOfType([PT.string, PT.number]),
    className: PT.string
};

(AlertStripe as React.ComponentClass).defaultProps = {
    className: undefined,
    size: '1.5em',
    inline: false
};

export default AlertStripe;

export { default as AlertStripeInfo } from './info-alertstripe';
export { default as AlertStripeInfoInline } from './info-inline-alertstripe';

export { default as AlertStripeSuksess } from './suksess-alertstripe';
export { default as AlertStripeSuksessInline } from './suksess-inline-alertstripe';

export { default as AlertStripeAdvarsel } from './advarsel-alertstripe';
export { default as AlertStripeAdvarselInline } from './advarsel-inline-alertstripe';

export { default as AlertStripeFeil } from './feil-alertstripe';
export { default as AlertStripeFeilInline } from './feil-inline-alertstripe';

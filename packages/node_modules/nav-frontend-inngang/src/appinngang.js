import React, { Component } from 'react';
import PT from 'prop-types';
import { Innholdstittel } from 'nav-frontend-typografi';

export default class AppInngang extends Component { // eslint-disable-line react/prefer-stateless-function
    render() {
        return (
            <div {... this.props} className="inngang inngang__app">
                <Innholdstittel>{ this.props.title }</Innholdstittel>
            </div>
        );
    }
}

AppInngang.propTypes = {
    title: PT.string.isRequired
};

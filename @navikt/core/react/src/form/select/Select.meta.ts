import type { ComponentMetadata } from "../../utils/types/metadata";
import { Select } from "./index";

const metadata: ComponentMetadata = {
  name: "Select",
  components: {
    Select,
  },
  keywords: ["select", "nedtrekksliste", "dropdown", "form", "input"],
  related: ["Combobox"],
};

export { metadata };

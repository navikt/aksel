import type { ComponentMetadata } from "../utils/types/metadata";
import { Button } from "./index";

const metadata: ComponentMetadata = {
  name: "Button",
  components: {
    Button,
  },
  keywords: ["button", "knapp", "cta", "action", "submit"],
};

export { metadata };

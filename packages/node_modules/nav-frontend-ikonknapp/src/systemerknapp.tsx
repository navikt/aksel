import * as React from 'react';
import Ikon from 'nav-frontend-ikoner-assets';

import Ikonknapp from './ikonknapp';
import { IkonknappProps } from './ikonknapp';

class Systemerknapp extends React.Component<IkonknappProps> {
    render() {
        return (
        	<Ikonknapp
        		ikon={<Ikon kind="systemer" />}
        		type="flat"
        		bareIkon
        		{...this.props}
        	/>
        );
    }
}

export default Systemerknapp;

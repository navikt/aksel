import type { ComponentMetadata } from "../utils/types/metadata";
import { Tag } from "./index";

const metadata: ComponentMetadata = {
  name: "Tag",
  components: {
    Tag,
  },
  keywords: ["tag", "merkelapp", "label", "badge", "chip"],
};

export { metadata };

import React from 'react';
import { SkjemaGruppe, Checkbox } from './../';
import generateSample from '../../../../guideline-app/app/utils/sampling/sampleDataGenerator';

const options = [
    { label: 'Bakerst', value: 'bakerst', id: 'bakerst-checkbox', name: 'bakerst', key: 'bakerst' },
    { label: 'Fremst', value: 'fremst', id: 'fremst-checkbox', name: 'fremst', key: 'fremst' },
    { label: 'Midten', value: 'midten', id: 'midten-checkbox', name: 'midten', key: 'midten' }
];

const checkboxChildren = (
    options.map((checkboxChildProps) => (<Checkbox {...checkboxChildProps} />))
);

export default generateSample({
    baseType: SkjemaGruppe,
    modifierNames: ['feil'],
    attrs: { title: 'Hvor vil du sitte?' },
    children: checkboxChildren
});

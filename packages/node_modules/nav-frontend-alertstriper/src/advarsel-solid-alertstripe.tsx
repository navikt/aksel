import * as React from 'react';
import AlertStripe, { AlertStripeProps } from './alertstripe';

class AlertStripeAdvarselSolid extends React.Component<AlertStripeProps> {
    render() {
        return <AlertStripe type="advarsel" solid={true} {...this.props} />;
    }
}

export default AlertStripeAdvarselSolid;

import Hjelpetekst from './';
import generateSample from '../../../guideline-app/app/utils/sampling/sampleDataGenerator';

export default generateSample({
    baseType: Hjelpetekst,
    attrs: { id: 'hjelpetekst' },
    children: 'Her har du et eksempel på en Hjelpetekst'
});

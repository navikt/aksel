import React from 'react';
import Chevron from './chevron';

function VenstreChevron(props) {
    return (
        <Chevron orientasjon="venstre" {...props} />
    );
}

VenstreChevron.propTypes = {};

export default VenstreChevron;

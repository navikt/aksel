import * as React from 'react';
import Ikon from 'nav-frontend-ikoner-assets';
import KnappBase, { KnappBaseProps } from 'nav-frontend-knapper';

const Lukknapp = (props: KnappBaseProps) => {
    return (
        <KnappBase type="flat" form="kompakt" {...props}>
            <Ikon kind="x" />
            {props.children || <span className="sr-only">Lukk</span>}
        </KnappBase>
    );
};

export default Lukknapp;

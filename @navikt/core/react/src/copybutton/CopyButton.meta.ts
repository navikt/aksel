import type { ComponentMetadata } from "../utils/types/metadata";
import { CopyButton } from "./index";

const metadata: ComponentMetadata = {
  name: "CopyButton",
  components: {
    CopyButton,
  },
  keywords: ["copy", "kopier", "clipboard", "button", "utklippstavle"],
  related: ["Button"],
};

export { metadata };

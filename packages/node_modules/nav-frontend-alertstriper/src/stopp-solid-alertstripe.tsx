import * as React from 'react';
import AlertStripe, { AlertStripeProps, AlertStripeType } from './alertstripe';

class AlertStripeStopp extends React.Component<AlertStripeProps> {
    render() {
        return <AlertStripe type={AlertStripeType.stopp} solid={true}  {...this.props } />;
    }
}

export default AlertStripeStopp;

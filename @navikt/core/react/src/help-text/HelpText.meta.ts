import type { ComponentMetadata } from "../utils/types/metadata";
import { HelpText } from "./index";

const metadata: ComponentMetadata = {
  name: "HelpText",
  components: {
    HelpText,
  },
  keywords: ["help text", "hjelpetekst", "tooltip", "hjelp", "forklaring"],
  related: ["Tooltip"],
};

export { metadata };

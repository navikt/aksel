import type { ComponentMetadata } from "../utils/types/metadata";
import { Loader } from "./index";

const metadata: ComponentMetadata = {
  name: "Loader",
  components: {
    Loader,
  },
  keywords: ["loader", "spinner", "loading", "laster", "progress"],
};

export { metadata };

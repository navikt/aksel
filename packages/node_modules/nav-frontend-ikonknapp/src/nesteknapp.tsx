import * as React from 'react';
import Ikon from 'nav-frontend-ikoner-assets';

import Ikonknapp from './ikonknapp';
import { IkonknappProps } from './ikonknapp';

class Nesteknapp extends React.Component<IkonknappProps> {
    render() {
        return (
        	<Ikonknapp
        		ikon={<Ikon kind="chevron-høyre" />}
        		type="flat"
                kompakt
        		ikonHøyre
        		{...this.props}
        	>
                {this.props.children || 'Neste'}
            </Ikonknapp>
        );
    }
}

export default Nesteknapp;

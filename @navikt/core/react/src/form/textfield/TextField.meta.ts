import type { ComponentMetadata } from "../../utils/types/metadata";
import { TextField } from "./index";

const metadata: ComponentMetadata = {
  name: "TextField",
  components: {
    TextField,
  },
  keywords: ["text field", "tekstfelt", "input", "form", "text"],
  related: ["Textarea"],
};

export { metadata };

import * as React from 'react';
import KnappBase from './index';
import { KnappProps } from './knapp';

class Fareknapp extends React.Component<KnappProps> {
    render() {
        return <KnappBase {...this.props} type="fare" />;
    }
}

export default Fareknapp;

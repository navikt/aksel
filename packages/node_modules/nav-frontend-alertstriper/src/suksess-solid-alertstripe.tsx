import * as React from 'react';
import { StatelessComponent } from 'react';
import AlertStripe, { AlertStripeProps } from './alertstripe';

const AlertStripeSuksessSolid: StatelessComponent<AlertStripeProps> = props => (
    <AlertStripe type="suksess" solid {...props} />
);

AlertStripeSuksessSolid.propTypes = {};

export default AlertStripeSuksessSolid;

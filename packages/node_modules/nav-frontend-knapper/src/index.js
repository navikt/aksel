import React, { PropTypes as PT } from 'react';
import 'nav-frontend-knapper-style'; // eslint-disable-line import/extensions

function NavFrontendKnapper({ children }) {
    return <button className="nav-frontend-knapper">{children}</button>;
}

NavFrontendKnapper.propTypes = {
    children: PT.node.isRequired
};

export default NavFrontendKnapper;

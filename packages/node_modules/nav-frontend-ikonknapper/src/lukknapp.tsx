import * as React from 'react';
import Ikon from 'nav-frontend-ikoner-assets';
import KnappBase, { KnappBaseProps } from 'nav-frontend-knapper';

class Lukknapp extends React.Component<KnappBaseProps> {
    render() {
        return (
            <KnappBase type="flat" form="kompakt" {...this.props}>
                <Ikon kind="x" />
                <span className="sr-only">{this.props.children || 'Lukk'}</span>
            </KnappBase>
        );
    }
}

export default Lukknapp;

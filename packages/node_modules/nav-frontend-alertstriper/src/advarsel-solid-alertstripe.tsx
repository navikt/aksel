import * as React from 'react';
import { StatelessComponent } from 'react';
import AlertStripe, {AlertStripeProps} from './alertstripe';

const AlertStripeAdvarselSolid: StatelessComponent<AlertStripeProps> = props => (
    <AlertStripe type="advarsel" solid {...props} />
);

AlertStripeAdvarselSolid.propTypes = {};

export default AlertStripeAdvarselSolid;

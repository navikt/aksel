import type { ComponentMetadata } from "../utils/types/metadata";
import { Theme } from "./index";

const metadata: ComponentMetadata = {
  name: "Theme",
  components: {
    Theme,
  },
  keywords: ["theme", "tema", "darkside", "color", "styling"],
  related: ["Provider"],
};

export { metadata };

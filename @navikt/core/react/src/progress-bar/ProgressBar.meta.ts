import type { ComponentMetadata } from "../utils/types/metadata";
import { ProgressBar } from "./index";

const metadata: ComponentMetadata = {
  name: "ProgressBar",
  components: {
    ProgressBar,
  },
  keywords: ["progress bar", "fremdrift", "loading", "progress", "status"],
  related: ["Loader", "Skeleton"],
};

export { metadata };

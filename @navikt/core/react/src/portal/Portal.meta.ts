import type { ComponentMetadata } from "../utils/types/metadata";
import { Portal } from "./index";

const metadata: ComponentMetadata = {
  name: "Portal",
  components: {
    Portal,
  },
  keywords: ["portal", "overlay", "render", "dom", "teleport"],
};

export { metadata };

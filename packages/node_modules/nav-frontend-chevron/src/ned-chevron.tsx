import * as React from 'react';
import Chevron, { NavFrontendChevronProps } from './chevron';

function NedChevron(props: NavFrontendChevronProps) {
    return <Chevron {...props} type="ned" />;
}

(NedChevron as any).propTypes = {};

export default NedChevron;

import React from 'react';
import { storiesOf } from '@kadira/storybook';
import './src/index.less';

storiesOf('Hjelpetekst', module)
    .add('Hjelpetekst', () => (
        <div className="hjelpetekst">
            Arial 14 px var oppå landet etsteds, og ingen kunne få malt der, for der var så fullt av trollskap. Ikke kunne få lov att male der
        </div>
    ));

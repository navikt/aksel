import * as React from 'react';
import { StatelessComponent } from 'react';
import AlertStripe, { AlertStripeProps } from './alertstripe';

const AlertStripeSuksess: StatelessComponent<AlertStripeProps> = (props) => (
    <AlertStripe type="suksess" {...props} />
);

AlertStripeSuksess.propTypes = {};

export default AlertStripeSuksess;

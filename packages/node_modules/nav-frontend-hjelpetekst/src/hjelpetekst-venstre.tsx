import * as React from 'react';
import HjelpetekstBase, { HjelpetekstProps } from './hjelpetekst';
import { StatelessComponent } from 'react';

const HjelpetekstVenstre: StatelessComponent<HjelpetekstProps> = (props) => <HjelpetekstBase type="venstre" {...props} />; // tslint:disable-line:max-line-length

export default HjelpetekstVenstre;

---
title: Teknisk
rank: 2
---

import { Bash, copyImport } from "../../../components/code/Code";
import "../../../components/code/styles.less";

## Installering

Alle fargene våre er [definert](https://github.com/navikt/nav-frontend-core/blob/master/less/_variabler.less) i
pakken `nav-frontend-core`, og kan installeres slik:

<Bash onClick={(e) => copyImport(e, "npm install nav-frontend-core --save")}>
  npm install nav-frontend-core --save
</Bash>
<br />
<Bash onClick={(e) => copyImport(e, "yarn add nav-frontend-core")}>
  yarn add nav-frontend-core
</Bash>

## Bruk med Less

Hvis du bruker Less kan du bruke disse variablene ved å importere en referanse til `_variabler`-fila i cora-pakken i stilarket ditt:

```less accordion=false
@import (reference) "~nav-frontend-core/less/_variabler";

.minDiv {
  background: @navRod;
}
```

import React, { Component } from 'react';
import { Textarea } from '../../nav-frontend-skjema';
import ValidBase from './';

class ValidTextarea extends Component { // eslint-disable-line react/prefer-stateless-function
    render() {
        const { ...other } = this.props;
        return (
            <ValidBase component={Textarea} {...other} />
        );
    }
}

export default ValidTextarea;

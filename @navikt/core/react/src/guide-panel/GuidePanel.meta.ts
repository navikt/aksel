import type { ComponentMetadata } from "../utils/types/metadata";
import { GuidePanel } from "./index";

const metadata: ComponentMetadata = {
  name: "GuidePanel",
  components: {
    GuidePanel,
  },
  keywords: ["guide panel", "veiledning", "hjelp", "guide", "panel"],
};

export { metadata };

import * as React from 'react';
import { StatelessComponent } from 'react';
import AlertStripe, { AlertStripeProps } from './alertstripe';

const AlertStripeInfoSolid: StatelessComponent<AlertStripeProps> = (props) => (
    <AlertStripe type="info" solid {...props} />
);

AlertStripeInfoSolid .propTypes = {};

export default AlertStripeInfoSolid;

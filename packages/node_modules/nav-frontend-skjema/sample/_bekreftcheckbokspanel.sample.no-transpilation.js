import React from 'react';
import { BekreftCheckboksPanel } from './../';

class BekreftCheckboksPanelExample extends React.Component {
    constructor(props) {
        super(props);
        this.state = { checked: false };
    }

    onChange(event) {
        // eslint-disable-next-line
        console.log('event', event);
        this.setState({ checked: !this.state.checked });
    }

    render() {
        return (
            <BekreftCheckboksPanel
                label="Jeg godtar"
                checked={this.state.checked}
                onChange={(event) => this.onChange(event)}
            >
                <span>Her kan du sende inn html. For eksempel en <a href="/lenke">lenke</a></span>
            </BekreftCheckboksPanel>
        );
    }
}

export default BekreftCheckboksPanelExample;

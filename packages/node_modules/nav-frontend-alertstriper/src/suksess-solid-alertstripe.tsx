import * as React from 'react';
import AlertStripe, { AlertStripeProps, AlertStripeType } from './alertstripe';

class AlertStripeSuksessSolid extends React.Component<AlertStripeProps> {
    render() {
        return <AlertStripe type={AlertStripeType.suksess} solid={true} {...this.props} />;
    }
}

export default AlertStripeSuksessSolid;

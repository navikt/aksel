import * as React from 'react';
import KnappBase from './index';
import { KnappProps } from './knapp';

class Hovedknapp extends React.Component<KnappProps> { // eslint-disable-line react/prefer-stateless-function
    render() {
        return <KnappBase {...this.props} type="hoved" />;
    }
}

export default Hovedknapp;

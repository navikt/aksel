import * as React from 'react';
import AlertStripe, {AlertStripeProps} from './alertstripe';

class AlertStripeSuksessSolid extends React.Component<AlertStripeProps> {
    render() {
        return <AlertStripe type="suksess" solid {...this.props} />;
    }
}

export default AlertStripeSuksessSolid;

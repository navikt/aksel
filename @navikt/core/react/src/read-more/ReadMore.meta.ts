import type { ComponentMetadata } from "../utils/types/metadata";
import { ReadMore } from "./index";

const metadata: ComponentMetadata = {
  name: "ReadMore",
  components: {
    ReadMore,
  },
  keywords: ["read more", "les mer", "expandable", "collapsible", "toggle"],
  related: ["Accordion"],
};

export { metadata };

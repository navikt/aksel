import * as React from 'react';
import AlertStripe, { AlertStripeProps } from './alertstripe';

class AlertStripeAdvarselInline extends React.Component<AlertStripeProps> {
    render() {
        return <AlertStripe type="advarsel" inline={true} {...this.props} />;
    }
}

export default AlertStripeAdvarselInline;

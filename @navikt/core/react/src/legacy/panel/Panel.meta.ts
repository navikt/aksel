import type { ComponentMetadata } from "../../utils/types/metadata";
import { Panel } from "./index";

const metadata: ComponentMetadata = {
  name: "Panel",
  components: {
    Panel,
  },
  keywords: ["panel", "container", "surface", "box", "wrapper"],
};

export { metadata };

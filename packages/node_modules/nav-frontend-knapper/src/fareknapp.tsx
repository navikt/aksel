import * as React from 'react';
import KnappBase, { KnappType } from './index';
import { KnappProps } from './knapp';

class Fareknapp extends React.Component<KnappProps> { // eslint-disable-line react/prefer-stateless-function
    render() {
        return <KnappBase {...this.props} type={KnappType.fare} />;
    }
}

export default Fareknapp;

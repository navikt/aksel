import * as React from 'react';
import { StatelessComponent } from 'react';
import AlertStripe, { AlertStripeProps } from './alertstripe';

const AlertStripeStopp: StatelessComponent<AlertStripeProps> = props => (
    <AlertStripe type="stopp" {...props} />
);

AlertStripeStopp.propTypes = {};

export default AlertStripeStopp;

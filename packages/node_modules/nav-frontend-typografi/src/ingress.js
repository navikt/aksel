import React, { Component } from 'react';
import TypografiBase from './index';

class Ingress extends Component { // eslint-disable-line react/prefer-stateless-function
    render() {
        return (<TypografiBase type="ingress" {...this.props} />);
    }
}

export default Ingress;

import type { ComponentMetadata } from "../../utils/types/metadata";
import { Fieldset } from "./index";

const metadata: ComponentMetadata = {
  name: "Fieldset",
  components: {
    Fieldset,
  },
  keywords: ["fieldset", "form", "group", "gruppe", "legend"],
};

export { metadata };

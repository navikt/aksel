import * as React from 'react';
import KnappBase from './index';
import { KnappProps } from './knapp';

class Flatknapp extends React.Component<KnappProps> { // eslint-disable-line react/prefer-stateless-function
    render() {
        return <KnappBase type="flat" {...this.props} />;
    }
}

export default Flatknapp;

import * as React from 'react';
import AlertStripe, {AlertStripeProps} from './alertstripe';

function AlertStripeInfo(props: AlertStripeProps) {
    return <AlertStripe type="info" {...props} />;
}

(AlertStripeInfo as any).propTypes = {};

export default AlertStripeInfo;

import * as React from 'react';
import { StatelessComponent } from 'react';
import AlertStripe, { AlertStripeProps } from './alertstripe';

const AlertStripeAdvarsel: StatelessComponent<AlertStripeProps> = props => (
    <AlertStripe type="advarsel" {...props} />
);

AlertStripeAdvarsel.propTypes = {};

export default AlertStripeAdvarsel;

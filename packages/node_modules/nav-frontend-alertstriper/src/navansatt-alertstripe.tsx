import * as React from 'react';
import AlertStripe, {AlertStripeProps} from './alertstripe';

class AlertStripeNavAnsatt extends React.Component<AlertStripeProps> {
    render() {
        return <AlertStripe type="nav-ansatt" size="5em" {...this.props} />;
    }
}

export default AlertStripeNavAnsatt;

import * as React from 'react';
import AlertStripe, {AlertStripeProps} from './alertstripe';

function AlertStripeAdvarsel(props: AlertStripeProps) {
    return (<AlertStripe type="advarsel" {...props} />);
}

(AlertStripeAdvarsel as any).propTypes = {};

export default AlertStripeAdvarsel;

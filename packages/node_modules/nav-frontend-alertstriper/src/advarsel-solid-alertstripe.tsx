import * as React from 'react';
import AlertStripe, { AlertStripeProps, AlertStripeType } from './alertstripe';

class AlertStripeAdvarselSolid extends React.Component<AlertStripeProps> {
    render() {
        return <AlertStripe type={AlertStripeType.advarsel} solid={true} {...this.props} />;
    }
}

export default AlertStripeAdvarselSolid;

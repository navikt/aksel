import * as React from 'react';
import AlertStripe, {AlertStripeProps} from './alertstripe';

function AlertStripeNavansatt(props: AlertStripeProps) {
    return (<AlertStripe type="nav-ansatt" size="5em" {...props} />);
}

(AlertStripeNavansatt as any).propTypes = {};

export default AlertStripeNavansatt;

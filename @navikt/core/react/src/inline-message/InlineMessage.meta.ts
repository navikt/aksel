import type { ComponentMetadata } from "../utils/types/metadata";
import { InlineMessage } from "./index";

const metadata: ComponentMetadata = {
  name: "InlineMessage",
  components: {
    InlineMessage,
  },
  keywords: ["inline message", "melding", "message", "feedback", "varsel"],
  related: ["Alert"],
};

export { metadata };

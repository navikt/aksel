import LenkeEksempel from './_lenke.example';
import generateSample from '../../../guideline-app/app/utils/sampling/sampleDataGenerator';

export default generateSample({
    baseType: LenkeEksempel,
    modifierNames: ['frittstaende'],
    tabOptions: { react: { show: false } }
});

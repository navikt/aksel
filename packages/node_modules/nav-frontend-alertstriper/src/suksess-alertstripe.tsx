import * as React from 'react';
import AlertStripe, {AlertStripeProps} from './alertstripe';

function AlertStripeSuksess(props: AlertStripeProps) {
    return <AlertStripe type="suksess" {...props} />;
}

(AlertStripeSuksess as React.StatelessComponent).propTypes = {};

export default AlertStripeSuksess;

import * as React from 'react';
import AlertStripe, { AlertStripeProps } from './alertstripe';

class AlertStripeInfoSolid extends React.Component<AlertStripeProps> {
    render() {
        return <AlertStripe type="info" solid={true} {...this.props} />;
    }
}

export default AlertStripeInfoSolid;

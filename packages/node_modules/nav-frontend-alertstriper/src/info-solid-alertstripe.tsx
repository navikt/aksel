import * as React from 'react';
import AlertStripe, {AlertStripeProps} from './alertstripe';

function AlertStripeInfoSolid(props: AlertStripeProps) {
    return <AlertStripe type="info" solid {...props} />;
}

(AlertStripeInfoSolid as any).propTypes = {};

export default AlertStripeInfoSolid;

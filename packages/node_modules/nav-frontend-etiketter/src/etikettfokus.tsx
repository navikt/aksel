import * as React from 'react';
import { StatelessComponent } from 'react';
import EtikettBase, {EtikettBaseProps} from './index';

const EtikettFokus: StatelessComponent<EtikettBaseProps> = props => (
    <EtikettBase type="Fokus" {...props} />
);

EtikettFokus.propTypes = {};

export default EtikettFokus;

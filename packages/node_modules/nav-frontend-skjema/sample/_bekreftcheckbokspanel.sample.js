// eslint-disable-next-line
import exampleCode from 'raw-loader!./_bekreftcheckbokspanel.sample.no-transpilation';
// eslint-disable-next-line
import BekreftCheckboksPanelExample from 'babel-loader!./_bekreftcheckbokspanel.sample.no-transpilation';

import generateSample from '../../../../guideline-app/app/utils/sampling/sampleDataGenerator';

export default generateSample({
    baseType: BekreftCheckboksPanelExample,
    tabOptions: {
        react: { show: false },
        html: { show: false },
        css: { show: false },
        js: { show: true, code: exampleCode }
    }
});

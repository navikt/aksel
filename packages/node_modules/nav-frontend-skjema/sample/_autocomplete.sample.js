// eslint-disable-next-line
import exampleCode from 'raw-loader!./_autocomplete.sample.no-transpilation';
// eslint-disable-next-line
import AutocompleteExample from 'babel-loader!./_autocomplete.sample.no-transpilation';

import generateSample from '../../../../guideline-app/app/utils/sampling/sampleDataGenerator';

export default generateSample({
    baseType: AutocompleteExample,
    tabOptions: {
        react: { show: false },
        html: { show: false },
        css: { show: false },
        js: { show: true, code: exampleCode }
    }
});

import type { ComponentMetadata } from "../../utils/types/metadata";
import { Textarea } from "./index";

const metadata: ComponentMetadata = {
  name: "Textarea",
  components: {
    Textarea,
  },
  keywords: ["textarea", "tekstområde", "input", "form", "multiline"],
  related: ["TextField"],
};

export { metadata };

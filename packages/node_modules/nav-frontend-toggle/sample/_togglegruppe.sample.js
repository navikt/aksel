import React from 'react';
import { ToggleGruppe } from './../';
import generateSample from '../../../../guideline-app/app/utils/sampling/sampleDataGenerator';

export default generateSample({
    baseType: ToggleGruppe,
    attrs: {
        toggles: [
            { children: 'B' },
            { children: (<em style={{ paddingLeft: 2, paddingRight: 2 }}>I</em>) },
            { children: (<span style={{ textDecoration: 'underline' }}>U</span>) }
        ],
        defaultPressed: 0
    },
    modifierNames: ['kompakt', 'multiSelect'],
    tabOptions: {
        react: { show: true },
        html: { show: true },
        css: { show: true }
    }
});

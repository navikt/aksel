import * as React from 'react';
import AlertStripe, {AlertStripeProps} from './alertstripe';

function AlertStripeAdvarselSolid(props: AlertStripeProps) {
    return (<AlertStripe type="advarsel" solid {...props} />);
}

(AlertStripeAdvarselSolid as any).propTypes = {};

export default AlertStripeAdvarselSolid;

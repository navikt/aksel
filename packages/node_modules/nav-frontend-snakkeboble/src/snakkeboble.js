import React, { Component, PropTypes as PT } from 'react';

class NavFrontendSnakkeboble extends Component {
    render() {
        const { children } = this.props;
        return (
            <div className="nav-frontend-snakkeboble">
                <h2>Hello from nav-frontend-snakkeboble</h2>
                {children}
            </div>
        );
    }
}

NavFrontendSnakkeboble.propTypes = {
    children: PT.node
};

export default NavFrontendSnakkeboble;

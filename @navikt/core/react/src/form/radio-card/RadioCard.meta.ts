import type { ComponentMetadata } from "../../utils/types/metadata";
import { RadioCard, RadioCardGroup } from "./index";

const metadata: ComponentMetadata = {
  name: "RadioCard",
  components: {
    RadioCard,
    RadioCardGroup,
  },
  keywords: ["radio", "radioknapp", "form", "input", "choice", "card"],
  related: ["Radio"],
};

export { metadata };

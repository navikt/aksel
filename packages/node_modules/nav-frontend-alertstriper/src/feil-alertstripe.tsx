import * as React from 'react';
import AlertStripe, { AlertStripeProps, AlertStripeType } from './alertstripe';

class AlertStripeFeil extends React.Component<AlertStripeProps> {
    render() {
        return <AlertStripe type={AlertStripeType.feil}  {...this.props} />;
    }
}

export default AlertStripeFeil;

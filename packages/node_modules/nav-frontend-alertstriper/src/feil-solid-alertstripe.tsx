import * as React from 'react';
import AlertStripe, { AlertStripeProps } from './alertstripe';

class AlertStripeFeilSolid extends React.Component<AlertStripeProps> {
    render() {
        return <AlertStripe type="feil" solid={true} {...this.props} />;
    }
}

export default AlertStripeFeilSolid;

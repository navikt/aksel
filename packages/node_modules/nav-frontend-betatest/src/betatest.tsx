import * as React from 'react';
import * as PT from 'prop-types';
import * as classnames from 'classnames';

export interface NavFrontendBetatestProps {
    /**
     * Egendefinert klassenavn.
     */
    className?: string;
}

class NavFrontendBetatest extends React.Component<NavFrontendBetatestProps> {
    render() {
        const { children, className } = this.props;
        return (
            <div className={classnames('nav-frontend-betatest', className)}>
                <h2>Hello from nav-frontend-betatest</h2>
                {children}
            </div>
        );
    }
}

(NavFrontendBetatest as React.ComponentClass).propTypes = {
    /**
     * Egendefinert klassenavn.
     */
    className: PT.string
};

export default NavFrontendBetatest;

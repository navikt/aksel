import React, { PropTypes as PT } from 'react';
import 'nav-frontend-knapper-style';

function NavFrontendKnapper() {
    return (
        <section>
            <h1>Todo</h1>
            <p>Se nav-frontend-knapper-style</p>
        </section>
    );
}

NavFrontendKnapper.propTypes = {
    children: PT.node.isRequired
};

export default NavFrontendKnapper;
